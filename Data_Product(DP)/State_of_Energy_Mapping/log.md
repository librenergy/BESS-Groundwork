# Log

## 2026-08-22

* **v0.2 — rebuilt from the first project clone's design review.** The `E_low`/`E_high` clamp-formula skeleton replaced by the matured design: MWh as the master unit with the percent-inversion warning (contractual 100% is physically "not full"; % is a derived metric, never a parameter); zero-anchored scales with buffers as knobs, not shifted anchors; §2 reshaped as a **function block** (inputs / outputs / knobs) with a 2×2 accounting-gain matrix (POM meter and BMS SOE, each direction its own gain) and a per-direction source selector — defaults charge = BMS SOE (charge efficiency shifts with power and temperature), discharge = POM meter at −1:1; consistency rules (installed ≥ contractual, no negative buffers) and three logic regions (top/bottom/in-between); §3 gains the data-path trace table (sources in / computation / output); §4 is the new **layered algorithm** — Layer 1 counting, Layer 2 steering (buffer targets, daily-tuned charge adjustment gain, asymmetric-buffer guidance from contract penalty asymmetry, manual rebalance mode with top buffer 0), Layer 3 recalibration (edge/power-roll-off triggers, tank-size vs SOE correction decision, logged and step-limited) — plus re-tuning over time and the monitoring dashboard list; §4 dynamics and §2 boundary-adjustment rows absorbed into the layers and gains. Ported genericized; project buffer values and diagrams stay clone-side. Drafting-QC pass applied.

* **v0.1 — template created.** New data product: the authoritative remap from installed state of energy (OEM-reported fleet energy) to contractual state of energy (the offtaker's 0-to-contracted range). Structure: two scales with high/low SoC buffers; `clamp((SOE_inst − E_low)/(E_high − E_low)) × range` parameter skeleton; computation-owner and telemetry-chain decisions; dynamics (degradation erodes buffers, augmentation re-parameterization, offline-unit treatment); contractual touchpoints (dispatch, capacity tests, energy-limited events, OEM self-assessment divergence). Ported from the first project clone, where the founding finding was a DIR mapping the offtaker SOC point straight to the OEM remaining-energy tag: no remap existed in the chain.