# Log

## 2026-08-25

* **Pre-publish visual re-audit.** The market-name and fictional-vendor residuals are accepted for publication under the maintainer's standing instruction that uploaded screenshots are pre-sanitized, per the image-audit judgment-call rule.

  **Open, not dispositioned:** the fleet-overview map plots a site marker adjacent to a real basemap place label. The standing instruction is a general one about screenshots and was not a confirmation about this artifact, which the maintainer has not been shown. Location is outside the judgment-call rule in any case: if the pin sits on the real asset's coordinates then the fictionalized site names do not anonymize anything, because the plant is identifiable from where it is drawn. Needs explicit confirmation that the coordinates are synthetic, or a re-crop that drops the basemap.

## 2026-08-17

* **Review edits (template pass).** Boundaries section removed (frontmatter description trimmed to match), status set to Template, register blanked into a fill-in template, platform section reframed as "example shown in Grafana (recommended)", tier headings sharpened (Site/BESS; analytics marked Advanced, digital twin of battery SoC and SoH), rule wording tightened. Review fixes on top: SoC typo in the Tier 4 heading, register intro reworded for the blank template, trailing whitespace trimmed, folder index aligned. Ported from the first project clone.

* **Generic pass (review edits).** One screenshot dropped from the Battery Alarm & Events Explorer section (`battery-alarm-events-explorer.png` removed from the assets; the time-series view remains). Project name removed from the document header: the specification is generic, not project-specific. 21 screenshots remain.

* **v0.2: the worked suite documented.** Reviewed 26 anonymized screenshots of the deployed Grafana suite, curated to 22 (four near-duplicate views left out: a second operational-overview scroll, a second-site power-distribution copy, an alarm-explorer copy with empty time series, a 24-hour twin view superseded by the 7-day one), renamed by tier and view, and filed in `dashboards.assets/`. Document restructured from the generic catalogue to the actual **four-tier suite**: fleet (Fleet Overview), site (Operational Overview, Availability, Power Distribution & Dispatch Tracking, Balancing, Telemetry & Energy Validation, Power vs Market Price), device (per-PCS PCS & Battery Balancing, PCS Fault & Status Explorer, PCS Health Explorer, PCS Transformer Health Explorer, Battery Alarm & Events Explorer), analytics (Daily Site Capacity waterfall, SoC Twin per Bus, Power Distribution per Node). Added the plant-hierarchy ID convention (PCS.1A1 / Bus.1 / Bat.1A1.4 / Str.5), a recurring-patterns section (KPI contract header, time-by-device heatmaps, same-metric-two-sources panels, contracted vs installed side by side, Pareto first, standard variable bar), and a 15-row dashboard register with links and metric/DIR mappings pending. Genericity: dataset fictionalized; market names and one fictional-vendor path accepted per the image-audit rule.

* **Specification v0.1 created.** Monitoring and diagnostic dashboards as a data product: five design rules (dashboards visualize metrics but never define them; diagnostic instruments, with the reports and GADS ledger as the numbers of record; drill-down follows the plant hierarchy; every panel deep-linkable so observations can cite charts as evidence; audience tiers as separate views), an eleven-dashboard recurring catalogue mapped to the observation log's fault families and the daily checklist's health dimensions, and a per-project dashboard register tying each built dashboard to its Metrics Tree and DIR IDs.
