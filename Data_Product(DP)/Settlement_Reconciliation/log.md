# Log

## 2026-08-25

* Genericised a meter example in the exceptions table: a named vendor's meter family became "some revenue meters", since the sentinel-value behaviour is not specific to one manufacturer and the template carries no vendor names.

## 2026-08-10

* **Moved under `Data_Product(DP)/`** from the repo root, acronym suffix dropped (`Settlement_Reconciliation(SR)` → `Settlement_Reconciliation`). On a non-ISO or tolling project the reconciliation is primarily the invoice walk rather than an ISO statement check, so scope it before the session.
