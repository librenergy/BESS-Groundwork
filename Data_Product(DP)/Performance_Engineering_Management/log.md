# Log

## 2026-08-25

* **Pre-publish visual re-audit; 2026-08-17 disposition reconfirmed.** The screenshot pass re-surfaced the residual market-service vendor names (an APM-vendor tag in the action-items tracker; an outage-management-ticket source with realistic ticket numbers and an ops file path in the outage event log; a market-services party in the engineering change tracker) plus team first names and the maintainer's own branding. Maintainer reconfirmed: screenshots are sanitized before upload and these residuals are deliberate leave-ins, accepted for publication per the image-audit judgment-call rule.

## 2026-08-17

* **Screenshots adopted from the project clone.** Six worked screenshots embedded one per register section, showing each register on a live deployment with a fictionalized dataset. Assets in `performance-engineering-management.assets/`. Visual genericity audit run: dataset fictionalized; residual weak identifiers (market-service vendor names, team first names) reviewed and accepted for publication by the maintainer.

* **v0.2 (review).** Daily log renamed daily performance review checklist; commercial event log renamed outage event log; site parameter registry removed (parameter history lives in the ECN old→new values); document register merged into the knowledge base. Sections renumbered 1–14.

* **Specification v0.1 created.** The management layer of performance engineering, distilled from a working client portal on another project (structure genericized — no client content): the register set and their field schemas, including the patterns that proved out in use — observation closures that state their destination (submitted / turned into action / turned into case / no further action, never a silent disappearance), due date + original due date to record slip, the per-contract excused flags plus a "should not be excused" dispute flag on the commercial event log, the ECN supersedes chain with plain-English summary and old→new values tied to a site parameter registry, case records carrying hypotheses and verification criteria with a serial-defect flag, and the daily log's review-status field as the proof somebody actually checked. Contacts deliberately excluded (the Contact Register is the source of record). Boundaries drawn to BESS GADS (commercial subset here, full ledger there), the Monthly Performance Report (reads the registers), the WOM (ECNs protect owner-side warranty conditions), and Work Products / SOPs (cases produce them).
