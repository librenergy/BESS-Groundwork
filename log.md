# Log

## 2026-08-13

* **New `genericity-audit` skill — the pre-contribution confidentiality check, codified.** Public skill (`.agents/skills/genericity-audit/`) capturing the audit routine developed during the pre-push work: scope is the working tree (all committable file types, `log.md` included) **plus every blob in git history** (a push publishes history, not just the tree); pattern classes cover names, PII/document artifacts, commercial figures, exact contractual values, executed dates, second-order contract-window fingerprints, and — the recurring leak class — **quoted worked examples** (restate lessons as patterns, never with the real figure that taught them). The split that makes it publishable: **the method is public, the vocabulary is private** — the skill reads a gitignored `.audit-wordlist.txt` each contributor maintains with their own client/project names, which never enters git. Includes the hard-won scanner warning (POSIX `git grep -E` silently drops `\b` and reports false-clean; verify against a known-dirty line). Registered as an AGENTS.md convention: run before any commit, PR, merge, or push that adds ported content. Self-applied on creation: skill and registration verified clean against the wordlist, wordlist verified invisible to git.

* **Pre-push audit — genericity, consistency, and the conventions AGENTS.md was missing.** Full sweep of the template before publishing the accumulated review-methodology work.

  **Genericity (three leaks found and closed).** A real LD-cap figure quoted verbatim from a client's executed agreement in the service-agreement archetype's page-verification rule — restated as the pattern (a truncated final digit) without the number; a vendor name embedded in an otherwise generic OEM rule ("*vendor*-does-not-guarantee style disclaimers") — rewritten as "the OEM's own 'not guaranteed as written' disclaimers"; and a vendor product name used as the unit-label example in the daily-report HTML skeleton. Verified clean afterwards across **every tracked and untracked file, all file types** (not just `.md`): no client, project, counterparty or vendor names, no contract figures, no executed-contract dates — including in `log.md`, which publishes with the repo.

  **Consistency.** The build sequence had drifted apart between its two homes: the README's prose chain named the battery supply agreement in the review order, but the `INSTRUCTIONS.md` §5 checklist had no step for it. Inserted as **step 5** (before the service agreement, since that agreement's formulas reference capacities the supply contract defines), **19 items renumbered to 24**, and the seven now-stale cross-references repaired (`Depends on step 9` → 10, `Needs steps 5 and 8` → 5, 6 and 9, the Metrics-Tree and Escalation dependencies, and both "Notes on the order" ranges). Also corrected the `Project_Documentation/index.md` claim of "**two** worked archetypes" — there are four (offtake, supply, service, OEM documentation), the exact enumerated-prose rot the three-pass sweep rule warns about; the root `index.md` gained the BSA domain and the PGM `calculations/` folder; the README's layer-1 row still described the two-companion review and now describes the archetype-plus-companion-set model.

  **AGENTS.md brought current.** The agent entry point carried none of the conventions developed over the last three sessions, so an agent reading it would not have learned the toolkit's current method. Added: **source documents are reviewed, not just filed** (the per-domain `instructions.md` read first, the four archetypes, the companion set, the two closing verification passes) and the **performance lens** (record what prices performance — LD/adjustment/buydown rates, caps, per-unit impact, and the dates that start performance clocks; purchase prices, payment schedules, credit support and delivery logistics stay out). The definitions bullet was extended with the **two-tier model** and the same-session roll-up trigger.

  **Conformance.** 154 internal links checked, zero broken; frontmatter conformant on every tracked `.md` (the only exceptions are the reserved `index`/`log`/`todo` files and the gitignored local `CLAUDE.md`).

  **Deep verification (second pass, same day).** Independent re-sweep with a wider vocabulary (people, addresses, emails/phones, DocuSign IDs, market constructs, equipment vendor stacks, contract-window numbers) across **all files including untracked**, plus — new — an exact scan of **every blob in the full git history**. Result: working tree fully clean; history carries exactly one pre-existing vendor product name in an HTML comment (already fixed in the working tree, already published in a prior push — left in history as not client-confidential). Market names (CAISO/ERCOT/WEIM/PJM) appear only as multi-market examples with adapt-to-your-market caveats, which is the intended pattern. History-blob scanning added to the pre-push genericity routine — POSIX `git grep` silently ignores `\b`, so the scan must run in a real regex engine or it reports false-clean.

## 2026-08-12

* **OEM-documentation review archetype added** (`Project_Documentation/OEM/instructions.md`) — the fourth archetype, for the non-contract documents: data sheets, system specifications, installation manuals. Three domain-specific rules: **version governance first** (the contract exhibits pin a governing spec revision; a newer/older standalone copy is a finding), no defined-terms pass, and quote the spec's own not-guaranteed-as-written disclaimers. Core method: ratings/envelope vs the contractual envelopes, and a **telemetry-granularity check against the warranty thresholds** — if a warranty counts components the customer telemetry cannot see, derive the observable workaround (proven on a real case: a module-counted serial-defect threshold vs unit-level telemetry, bridged by nominal-vs-available capability quantum steps). Response-time extraction with exact units. Ported from the first project clone.

* **New `Project_Documentation/BSA/` domain + supply-agreement review archetype** — the third worked `instructions.md` (after the offtake and service-agreement archetypes), with a supply lens: **sizing, quantity, and ratings are the spine**, and the document is the anchor the sibling contracts stand on (it typically defines the guaranteed capacities the service agreement references and the completion dates that start every downstream clock). Structure: scope-of-supply question first (**AC block vs DC block + separate PCS** — decides whether a PCS supply folder exists), definitions with the **capacity and defect vocabularies** taken verbatim (defect levels; **serial-defect** threshold/trigger/remedy/cost allocation), a design review (quantity, rated power, rated energy, **installed vs guaranteed capacity reconciled against `project_info.md`**), warranties, a commissioning review mapping **every completion date to the clocks it starts**, a SCADA/controls pass with a **full response-time review** (ms-vs-s units, ramp rates, gain/droop, cross-checked against offtake and interconnection requirements), and a keyword scan extended with a **technical set** (temperature, voltage, current, reactive/active power, frequency, ramp). Two new domain companions: `design.md` and `commissioning.md`. Domain registered in the Project_Documentation index, INSTRUCTIONS §4, and the README review order (supply before service — the service agreement's formulas reference its definitions).

## 2026-08-11

* **LTSA review archetype upgraded by merging a second project's proven method.** The second clone carried a hand-written `how-to-review-an-ltsa.md` (proven on a different OEM's LTSA) with method the archetype lacked; the two are now one document: **Step 0 extraction discipline** (`pdftotext -layout` — default extraction jumbles the tables that hold every substantive value; page-render verification; typos recorded as written), **exhibit inventory** as the first step (reserved vs referenced-but-unattached), the **seven questions asked of every guarantee**, capacity depth (BOL-headroom computation, reset/interpolation/permanent-adjustment rules, the what-is-the-percentage-*of* anchor question), the **augmentation section** (search for it, classify every hit — the word appearing is not an obligation existing — and record a verified absence as a finding, then ask who keeps the *offtake* capacity whole), exclusions **with carve-backs** ("an exclusion without its carve-back is a misquote"), a **liability/LD-cap step**, owner **silence-is-acceptance windows** ("the only obligations where doing nothing costs money"), and a closing checklist. Vendor names stripped; the archetype remains generic.

* **Five missing folder `index.md` stubs created** (Contact Register, DIR, RACI, WOM, ECP) — the every-folder-carries-an-index convention had never been applied to the master's own document folders; discovered while porting the structure to a second project clone.

## 2026-08-10

* **Source-document review methodology ported from the first project clone** — the backlog's largest outstanding item, developed over full reviews of an OEM service agreement and an offtake agreement.

  **`project-document-review` skill:** every `Project_Documentation/<DOMAIN>/` folder now carries an **`instructions.md`, read first** — the domain-specific method layered on the mechanical workflow (write one on first review if the folder has none). The contract companion set grew from two files to seven: `summary.md` (now explicitly carrying reporting obligations, personnel, communication protocols and costs — the generic sections every review forgets), `definitions.md` (**Tier 1**, extracted *before* anything else because defined terms span exhibits and sibling agreements), `guarantees.md` with mandatory sections, `operations-and-maintenance.md`, **`outage-management.md`** (offtake and grid agreements: excused-vs-charged by cause, notice-conditioned excuses, attribution control, system of record), `telemetry.md` with **measured vs calculated** marked per point, and **`red-flags.md`** written for the person who hasn't read the contract. Two closing **verification passes** — a keyword scan (obligation/communication/money/time) and a definitions cross-reference — both logged; the keyword scan is what catches fees outside the fee table. Step 6 now opens with the **definitions roll-up** into the master glossary, same session.

  **Two worked `instructions.md` archetypes** ship with the template, because the lens inverts between them: `LTSA/instructions.md` (the owner **holds** the guarantees — read as the claimant) and `PPA/instructions.md` (the owner **gives** them — read as the defendant; every guarantee an exposure, every reporting duty staffed work, counterparty numbers arriving with money already deducted). Each carries its own known-traps list. Any other domain writes one on first review.

* **`Performance_Guarantee_Matrix(PGM)/calculations/` scaffolded** — the skill had described calc sheets since 2026-07-25 but the folder didn't exist. Adds `calc-sheet-template.md` (the full section structure, with an empty section stated to be a finding rather than an omission) and a folder `index.md` carrying the **shared placeholder schema for the pseudo-SQL sections** — nine tables defined once so a populated project's sheets don't invent one naming convention each. The template's `## Pseudo-SQL` section ships with a worked event-based-availability query and the framing rules that stop it becoming a false source of truth: illustrative not runnable, placeholder names pending the Data Interface Register, **the formula wins on any disagreement**, and unknown constants as `contract_param` lookups marked ❓ rather than literals. The index records what writing the queries reliably surfaces — including **which conditions cannot be expressed in SQL at all** (deemed values, notice-deadline tests), which is the finding that scopes a build. `performance-guarantee-matrix` skill updated to point at both.

* **Build-sequence checklist added to `INSTRUCTIONS.md` (§5), and the README sequence realigned to match.** One order, expressed twice: the README as a prose dependency chain (13 steps), INSTRUCTIONS as a tickable 23-item checklist in four phases (inputs → vocabulary and parties → obligations, roles and measurement → data products). Ported from the first project clone, where the sequence was dictated from real build experience. Substantive changes to the documented order: **`project_info.md` is an explicit first step**; the per-document review order is named (design → offtake → OEM service → interconnection → O&M → EPC → SCADA/OEM specs); **Definitions & Taxonomy and the Contact Register now precede the EIM**, which holds because the parties come from the contracts and the Contact Register carries no *Source EIM version* stamp; the Warranty Obligation Matrix precedes the Performance Guarantee Matrix; **SCADA sets and OEM equipment specs became an explicit review step** (the Data Interface Register depends on them); and **BESS GADS moved ahead of the daily and monthly reports**, since both read its event ledger. A "Notes on the order" block records why each non-obvious placement is where it is, so a future clone can argue with the reasoning rather than just the list. `eim-review-build`'s next-steps offer rewritten to match.

* **Sweep discipline added to the AGENTS.md conventions** — two rules distilled from the folder move in the same session, both of which had just been learned the hard way. **(1) Renames and moves are three-pass sweeps:** links *to* the moved thing, links *from* it (relative paths silently change depth, and nothing pointing at the folder reveals them), and prose that *enumerates* the document set (build-sequence lists, "the ten documents" counts, `(repo root)` template-path notes) — the third pass rots quietly because it still reads fine. **(2) Never sweep `log.md`:** historical entries keep the names they were written with, since an entry recording a rename is *about* the old name; an earlier blanket sweep had rewritten one into claiming a folder was renamed from itself.

* **Definitions became a two-tier structure, with a dedicated sequence step.** Each reviewed source document carries its own `definitions.md` — everything *that document* defines, in its own words, with the clause reference — and the master glossary is the **roll-up** of those extractions rather than a parallel document. The `definitions-taxonomy` skill previously said the opposite ("one glossary for the whole repo — never per-document glossaries"), a rule the first project's contract reviews had already overtaken by writing one. The skill now carries: a **tier table** (Tier 1 extraction = source of record, written by `project-document-review`; Tier 2 master = single lookup, written by this skill), a **promotion rule** (acronyms; terms used outside their own document; anything naming money, a guarantee, a measurement boundary, a clock or a deadline; and mandatorily every word two contracts define differently — internal contract vocabulary stays in Tier 1), a **relinking convention** (Tier 2 Source cells link to the Tier 1 file, so master → extraction → signed PDF is two hops; Tier 1 files carry a back-link), and a **roll-up trigger** — a review that writes or changes an extraction rolls it up in the same session. The build procedure is reordered to read Tier 1 files *first* and treat the repo scan as a top-up, with a deliberate post-promotion collision check and a "when to run" list.

* **New README sequence step 4 — Definitions & Taxonomy**, between the EIM and the Contact Register (also in `INSTRUCTIONS.md` and the `eim-review-build` next-steps offer): all documents reviewed, so all extractions exist, so the glossary and naming conventions are settled **before** the registers are written. Step 2 now names `definitions.md` as a standard review output; layer 1 of the three-layer table lists it too. Sequence renumbered to 12 steps. Ported from the first project clone, where the LTSA review produced an extraction the glossary had no rule for. **Not yet ported:** the matching handoff in `project-document-review`, which depends on the pending five-companion methodology update.

* **Rewrote the README's recommended sequence** as one continuous dependency chain (11 steps) instead of a two-part "load the inputs, then build the documents" split. The chain now starts where the work actually starts: **load the project documents**, then **extract each into a `summary.md`** — the step that gives every later session its context, turning them from blank-page interviews into reviews of what the contracts already say — and only then the EIM. A callout records that **steps 2 and 3 can swap**: where someone holds the whole picture, the map can be built from the interview alone and the document review becomes the verification pass — faster to start, but only as good as memory.

* **Moved BESS GADS and Settlement Reconciliation into `Data_Product(DP)/`** — `BESS_GADS(GADS)/` → `Data_Product(DP)/BESS_GADS/` and `Settlement_Reconciliation(SR)/` → `Data_Product(DP)/Settlement_Reconciliation/`, **acronym suffixes dropped on the way in** per the top-level-only rule. Each is defined by what it recurs into: the outage/derate event ledger and the monthly shadow-settlement walk. The toolkit is now eight document templates plus four data products, and README step 11 carries the within-category build order (GADS → Settlement Reconciliation → Monthly Report), since the first two supply numbers the reports assemble. ⚠️ **Recorded caveat:** the split option (taxonomy/procedure stays a document, output becomes a product) was considered and not taken, so the GADS event-type and cause-family definitions that the Metrics Tree and Performance Guarantee Matrix depend on now sit inside a product folder — both are reference documents that produce a deliverable, not report templates, and the folder indexes say so. References swept (README, root index, both skills, glossary, metrics tree, CLAUDE.md); taxonomy §2b gained a retired-folder-acronym list (`MPR`, `OAP`/`GADS`, `SR`) and the rule that a folder moving under `Data_Product(DP)/` drops its suffix; historical log entries keep the old paths as written. A missing `index.md` was created for `Settlement_Reconciliation/` per the per-folder-index rule. No template content changed. Ported from the first project clone, where the question surfaced.

* **Sweep lesson worth keeping:** a folder move needs three passes, not one. Rewriting links *to* the moved folder is the obvious pass; the two that get missed are **links *from* the moved documents** (relative paths that silently change depth — `../Metrics_Tree(MT)/…` became `../../`) and **prose that enumerates the document set** (the `eim-review-build` skill's next-steps order, the glossary's "Satellite documents" row, "(repo root)" notes on template paths in the `monthly-performance-report` and `settlement-reconciliation` skills). All fixed here; `CLAUDE.md` also corrected, where the structure list still carried `Monthly_Performance_Report(MPR)` from the 2026-07-28 rename and the `metrics-and-kpis.md` merged away on 2026-07-29.

## 2026-08-08

* **Added [INSTRUCTIONS.md](/INSTRUCTIONS.md)** — a working guide for the people using a project clone, distinct from the README (index + build sequence) and AGENTS.md (agent entry point). Written **agent-optional**: every path through it works with nothing but a text editor, with agent sessions presented as an accelerator. Eight sections: what the repo is (the three layers), a find-what-you-need lookup table, how to read a document (the `index`/`log`/`todo`/`notes` folder shape, header stamps, ❓/⚠️ markers), adding a source document, running a build session by hand or by skill, the seven conventions that matter in practice, working with an agent (what to ask, how to review its output), and standing up a new clone. Ported from the first project clone, where the gap surfaced; project specifics stripped (the dual-definition warning is stated as the general offtake-vs-OEM pattern rather than a named project's).

* **Restructured the [README](/README.md) around the three layers** — project inputs (`project_info.md` + `Project_Documentation/`), toolkit documents, and data products — which is how a populated clone actually grows but was nowhere stated: the flat Resources table listed `project_info.md` last and `Project_Documentation/` not at all, despite both being where every session's inputs come from. New *How the repository is organised* section, Resources split into one table per layer, and the per-folder four-file shape (`index`/`log`/`todo`, plus `notes` where needed) stated once up front. Source-document review added as the sequence's step zero, the final step generalized from the Monthly Report to data products, and the agent section marked optional. Root index gained the INSTRUCTIONS pointer.

## 2026-08-01

* **Acronyms are folder-only — EIM file renamed** `Entity_Interaction_Map(EIM)/Entity_Interaction_Map(EIM).md` → `entity-interaction-map.md`, closing the sole outlier against the §3 kebab-case document rule (every other folder already followed it: `data-interface-register.md`, `performance-guarantee-matrix.md`, `bess-gads.md`…). Rule made explicit in [taxonomy §2b](/Definitions_Taxonomy%28DT%29/taxonomy.md); references swept (root index, README Resources table, `Metrics_Tree(MT)/metrics-tree.md`, EIM folder index, `eim-review-build` skill ×3, CLAUDE.md, historical log link targets).

## 2026-07-29

* **Metrics layer merged to one document.** `metrics-and-kpis.md` folded into `metrics-tree.md`, now titled **Metrics Tree & KPIs** — the tree leads, followed by the metric-vs-KPI preamble, the node-definition table, the metric definition sheet, the starter metric set, the KPI target table, source-of-truth and reconciliation rules, and the customization notes. Motivation from first project use: two files meant two version counters, duplicated headers, and a purple-KPI legend pointing into a separate document that drifted. The KPI-class legend and node-definition table now reference the in-document sheets (`Metric ref` column), and a consistency rule was added to the customization notes: every purple node must resolve to a metric definition in the same document. `metrics-tree` skill rewritten for the one-document shape (same three-part session: tree → metrics → KPIs); README sequence, Resources table (one row), and root index updated; references swept in the bess-gads and monthly-performance-report skills and the BESS GADS template. Historical log entries keep the old path as written.

* **Added the Daily Performance Report template** as the second data product in `Data_Product(DP)/`, then **rebuilt it against two production reports** (an HTML issue at generator v1.10 and a PDF issue at v1.15, both from a live daily-report pipeline). The rebuild replaced the first draft's contract-limit-tracking shape with the structure real daily reports actually use — the report is organised around **availability under each contractual definition**, not around usage limits:

  - **Four availability definitions plus a combined figure** — Equipment (Installed), Equipment (Contract), Power, Operational, and **Combined = min of Contract, Power, Operational** (deliberately excluding Equipment (Installed), since installed capacity above contract capacity is not an obligation). Each gets its own row; a project adapts the names but never blends them into one "availability".
  - **Two orthogonal presentation axes**, both carried on every value: **With Excused @ 100% vs Without Excused** (excused minutes credited at contract MW, with the unexcused figure in an italic sub-line — the pair is what a dispute turns on) and **Raw vs Depth-Weighted** (shallow-SOC forgiveness on a declared curve; equipment rows carry no depth-weighted value).
  - **KPI tile row**, availability summary table, five full-day charts (including **per-PCS** availability — what turns a headline number into a work order), Energy Summary with **both RTE figures** (SOC-matched and raw, which diverge sharply on a partial-cycle day), Data Quality listing **gap-affected units by name and percentage** rather than a site-level figure that hides them, **Commercial Events** (counterparty outage reference, MW down, excused verdict, including events starting before the day and overlapping it), and a **Report Parameters** block — site/contract, setpoint tolerance bands and ramp window, depth-weighting threshold and exponent — because without it the report is an assertion rather than something reproducible.
  - New companion **`daily-performance-report-template.html`**, completed to **full markup** on a second comparison pass against the source reports (the first cut had stubbed four sections and four of the five charts as HTML comments): all 11 `h2` sections present, the complete five-row Availability Summary with the Combined row's accent emphasis and the em-dash cells where equipment rows have no depth-weighted value, all **five** chart blocks, and real Energy Summary / Data Quality / Commercial Events / Report Parameters tables. Captures the generator's actual patterns — dark `#2d3748` table headers, alternating row panels, conditional red/green value spans, the `#fff3cd` warn panel on the RTE boundary row, `colspan=2` group headers in the parameter block, and 3-per-row KPI tiles as nested tables. This is the render skeleton the generator targets — 700px centred inline-styled table layout with no CSS classes (survives email clients and headless-Chromium PDF export), KPI tile and table patterns, base64 PNG chart embedding for a self-contained archivable file, and the palette.
  - A short **"Conventions to pin down before the first issue"** checklist retains the transferable rules from the earlier draft (measurement boundary per metric, cycle definition matching the OEM guarantee, day boundary across DST, provisional-vs-final and restatement, emit from the authoritative calculations rather than recomputing) and notes that a machine-readable counterparty feed is a *separate* product, since the counterparty usually supplies the schema.

  Registered in the folder index, root index, and README Resources table. Fully genericized — no site names, codes, figures, or dates from the source reports.

* **Fixed a stale acronym reference in the `data-product` skill** — step 5 still instructed agents to create `Data_Product(DP)/<Product_Name(ACR)>/` and register the acronym in the taxonomy, contradicting the top-level-only acronym rule adopted 2026-07-28. Any clone following it would have recreated exactly the nested-acronym folder name that rule removed. Product subfolders are plain-named; "external / counterparty data exchange" also added to the skill's list of typical products.

## 2026-07-28

* **Acronym suffixes are now top-level only.** `Monthly_Performance_Report(MPR)` renamed `Monthly_Performance_Report`: a folder nested inside an acronym-suffixed folder no longer carries its own acronym, because stacked acronyms in one path are harder to read than the names they abbreviate. Rule recorded in [taxonomy §2b](/Definitions_Taxonomy%28DT%29/taxonomy.md); `MPR` dropped from the folder registry and the `DP` row now notes that its product subfolders are plain-named. References swept (root index, README, `Data_Product(DP)/index.md`, the product's own `index.md`, `monthly-performance-report` skill ×2); historical log entries keep the old path as written.

## 2026-07-26

* **Public-release agnosticism pass:** audited every committable file for project-specific content — no names, figures, or dates found; genericized second-order fingerprints (contract-specific notice-window and command-timeout wording, `max(charge, discharge)` rule framing, a worked review anecdote, tree-pattern provenance phrasing); marked the PGM seed rows as examples; added market-adaptation caveats to the CAISO-style ISO signal names (EIM, MPR); fixed stale document names in the root index and README; resolved the undefined `OBE` label in the tree; removed the stale duplicate `metrics-tree.mermaid`; added the missing reserved `index.md` files (EIM, MT, PGM, GADS, MPR folders).

* **Folder↔skill reconciliation:** every document category now has a build skill. The `kpi-definitions` skill renamed **`metrics-tree`** (it builds the whole Metrics_Tree(MT) folder: tree + Metrics & KPIs) and `outage-availability-accounting` renamed **`bess-gads`**, matching the restructured folders; new **`data-product`** skill added for scoping products in Data_Product(DP) (the MPR keeps its dedicated skill). All slug references swept (README Resources table, skills, todo lists); README gained a Data Products category row. The Contact Register remains the one deliberate no-skill document (filled directly).

* **Reviewed and realigned all twelve skills** with the day's restructures: `kpi-definitions` rewritten as the **Metrics Tree, Metrics & KPIs session** (tree first, then per-contract metric definitions, then sparing KPI-target promotion; asset-agnostic note for PV transfer); `outage-availability-accounting` rewritten as the **BESS GADS session** (GADS-aligned event types with the MO/RS/EL/AUG traps, cause-code enumeration, per-contract verdicts, charge/discharge split, backfill test); `eim-review-build` next-steps updated to the ten-step sequence ending in the MPR data product, with a PV-transferability note; `monthly-performance-report` reframed as a data product with new doc references; stale "Obligation Matrix"/"KPI Definitions" references corrected in the escalation, PGM, DIR, and definitions-taxonomy skills; `settlement-reconciliation` gained the non-ISO/tolling note (invoice verification = the payment walk). READMEs: sequence synced, "build the tree first" made explicit, and a BESS-first-but-asset-agnostic line added to the purpose.

* **Restructured** the toolkit per first-project-use decisions, applied immediately per the porting policy: **(1)** KPI Definitions merged into `Metrics_Tree(MT)/` as the [Metrics & KPIs template](/Metrics_Tree%28MT%29/metrics-and-kpis.md) — metric (authoritative calculation) vs KPI (metric vs target) split, metric sheets with kpi_code/what-it-is-not/reporting-chain fields, starter metric set, KPI target table; `KPI_Definitions/` folder removed. **(2)** Outage & Availability Procedure revamped into [BESS GADS](/BESS_GADS%28GADS%29/bess-gads.md) (`BESS_GADS(GADS)/`) — a NERC-GADS-aligned BESS event taxonomy (PO/PE/MO/U1–U3/SF/RS/NC/D1–D4/PD plus BESS-specific EL and AUG; cause families; per-contract verdicts; charge/discharge MW split; ledger reconciliation) ready to export when NERC opens standalone BESS reporting. **(3)** New [Data Products](/Data_Product%28DP%29/index.md) parent folder (`Data_Product(DP)/`) with the Monthly Performance Report moved inside as the first product. README sequence, Resources table, AGENTS.md, local CLAUDE.md, taxonomy §2b, definitions template, and both affected skills updated; references swept.

* **Renamed** `RACI_Matrix` → `RACI_Matrix(RACI)` (completing the folder-acronym convention; references swept).

* **Aligned** all satellite templates with the session's content-level learnings: inline *Change log* tables removed from eight templates (history lives in each folder's `log.md`) and the DIR's Open Items table replaced with a `todo.md` pointer; EIM template typos fixed (Operational Metrics, Substation Spares, PCS OEM CMMS label, Offtaker/Provider comments); KPI sheet template gained `kpi_code`, *What it is / is not*, and *Reporting chain* fields plus the two-KPIs-per-dual-contract-definition rule; Contact Register gained the multi-role-counterparty guidance (one row per group/desk), a Technology Vendors section (SCADA vendor, APM vendor), and the notice-exhibit tip; DIR gained the integrated-OEM collapse rule and the non-ISO remapping note.

* **Renamed** the template folders to carry their ratified acronyms (`Entity_Interaction_Map(EIM)`, `Contact_Register(CR)`, `Data_Interface_Register(DIR)`, `Performance_Guarantee_Matrix(PGM)`, `Warranty_Obligation_Matrix(WOM)`, `Escalation_Communication_Plan(ECP)`, `BESS_GADS(GADS)`, `Monthly_Performance_Report(MPR)`, `Settlement_Reconciliation(SR)`, `Metrics_Tree(MT)`, `Definitions_Taxonomy(DT)`) with a repo-wide reference sweep (22 files; links percent-encode the parentheses) and the EIM template file renamed to match. Registry in the taxonomy template §2b. `KPI_Definitions`, `RACI_Matrix(RACI)`, and `Project_Documentation` unchanged.

* **Upgraded** the [Master Metrics Tree](/Metrics_Tree%28MT%29/metrics-tree.md) template with the conventions proven in field use: arithmetic edge operators (`+ − × ÷ Σ min`) making the money limbs a computable calculation graph; dashed couplings reframed as **guardrails**; an "Edge & owner semantics" section (owner tags per node from the RACI session, data-source leaves cross-referenced to DIR SYS/IF rows, contract refs as `%%` comments); a cloud working-copy option; and a much richer tolling-asset pattern in the customization notes (four limbs: revenue-minus-deductions drilled to equipment, OEM LD-recovery claim tree, shallow costs, warranty/guarantee preservation gate; offtake and OEM availability as fully independent KPIs). Template's inline change-log table removed (history lives in the folder `log.md`).
* **Added** a document/folder acronym registry to the [taxonomy template](/Definitions_Taxonomy%28DT%29/taxonomy.md) (§2b) — acronyms live in index display names only, never in folder paths; root index now shows (EIM)/(DIR)/(PGM).

* **Documented** the cloud working-copy pattern for the EIM (proven in field use): while drafting collaboratively, the diagram may live in a cloud Mermaid editor (Mermaid Chart / Mermaid.ai via MCP) as the single working copy — the repo file holds a pointer, the folder log/todo hold history and open items, snapshots sync back at milestones, and concurrent-edit rules apply (re-fetch before editing; refresh the editor after MCP pushes). Added to the [EIM template](/Entity_Interaction_Map%28EIM%29/entity-interaction-map.md) header and a new "Working copy" section in the `eim-review-build` skill. Local CLAUDE.md refreshed to match current conventions (edit-in-place outputs, todo/log session endings, definitions-first, Definitions_Taxonomy(DT) in the structure list, OKF v0.2 pointer).
* **Adopted** the per-folder `todo.md` convention: open items, pending questions, and unresolved conflicts live in a checklist file per folder (created when open items first accumulate), never in document bodies — main documents stay clean formal versions with a one-line pointer. Rule added to the AGENTS.md conventions; the `eim-review-build` wrap-up now records open questions in the folder's todo.md and session changes in its log.md.

* **Adopted** the per-folder change-log convention: folders whose documents evolve materially keep their own `log.md` (OKF-reserved structure, date-grouped, newest first), created on first material change; the root log carries repo-wide and cross-cutting changes plus pointers to major folder milestones. Rule added to the AGENTS.md conventions.
* **Updated** the repo's OKF reference from v0.1 to [v0.2](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md) (AGENTS.md conventions, glossary template) and audited conformance against the v0.2 spec: all non-reserved `.md` files carry frontmatter with a non-empty `type`, reserved `index.md`/`log.md` carry none, no retired `timestamp` keys. Fully conformant; the optional v0.2 trust/lifecycle fields are not yet adopted.
* **Added** the [Definitions & Taxonomy](/Definitions_Taxonomy%28DT%29/index.md) component: a [definitions.md](/Definitions_Taxonomy%28DT%29/definitions.md) template for the global glossary (every acronym/term/organization defined once with its project meaning; a dedicated ⚠️ ambiguous-terms section for double meanings like "SC" = Scheduling Coordinator vs Substantial Completion, per-contract "availability"/"RTE" variants, and multiple "capacities") and a [taxonomy.md](/Definitions_Taxonomy%28DT%29/taxonomy.md) template for naming authority (organization codes, canonical equipment terms, ID-prefix conventions agreed before the documents that need them). New skill [`definitions-taxonomy`](/.agents/skills/definitions-taxonomy/SKILL.md): populate definitions from a full-repo scan, maintain taxonomy via facilitated Q&A, with the standing rule that every new acronym gets defined in the session that introduces it. Registered in the README Resources table and root index.
* **Genericized** consultant-platform wording in the Data Interface Register template and skill: the ingestion-path example now reads "Modbus → collector → historian" and the table-prefix column is vendor-neutral, so project clones carry no references to any specific data-platform stack.
* **Wired** the Definitions & Taxonomy documents into every skill: all eleven document skills now open with a "Before you start" note directing sessions to read `definitions.md` and `taxonomy.md` first, use the established vocabulary, and add newly introduced acronyms to the glossary before wrap-up; the same rule was added to the AGENTS.md conventions. The `definitions-taxonomy` skill's scan rule was sharpened (every acronym plus any term whose project meaning isn't self-evident, worked category by category; plain English stays out).
* **Removed** the remaining consultant-platform references: the stale Lucidchart-workspace invite links in the README and the EIM template header, and the named-consultant example in the `eim-review-build` skill (now "a performance-engineering consultant"). The EIM template's authorship byline is the only remaining brand mention.

## 2026-07-25

* **Added** the [Project Documentation](/Project_Documentation/index.md) domain folder structure (PPA, LTSA, EPC, GRID, O&M, OEM, SCADA, EMS, Design, Asset_Management, Performance_Engineering), each with a per-folder index describing what belongs there — the landing zone the `project-document-review` skill files into.
* **Clarified** the template convention: the whole repo is cloned per project and templates are edited **in place** in the clone (one file per document, versioned inside the document); pristine bases live only here. README Resources intro and Contributing rewritten; all skill Outputs sections changed from save-a-copy naming to populate-in-place (also clearing stale `NN-` prefixes in skill output filenames).
* **Fixed** the Entity Interaction Map template's annotation-techniques table: escaped the raw pipes in the "Edge labels" example code span, which were breaking the Markdown table row.
* **Added** the `project-document-review` skill: turns client-provided source documents (PDF/Word/drawing sets) into indexed Markdown — a `summary.md` per document plus a `guarantees.md` extraction where guarantees/warranties/LDs are present — with per-folder indexing, verification of extracted values against page images, and handoff to the performance-guarantee-matrix and warranty-obligation-matrix skills.
* **Adopted** [OKF v0.1](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md): frontmatter on AGENTS.md, this log, and the root [index](/index.md); a Conventions (OKF) section in [AGENTS.md](/AGENTS.md) including the per-folder `index.md` rule.
* **Added** a [project_info.md](/project_info.md) template — a blank scaffold (with a dummy example project) for key project facts: capacity, equipment, commercial structure, performance-engineering value metrics, counterparties, milestones, and open items.
* **Split** the Warranty Obligation Matrix into two documents: the matrix re-scoped to [defect-style warranties only](/Warranty_Obligation_Matrix%28WOM%29/warranty-obligation-matrix.md), and a new [Performance Guarantee Matrix](/Performance_Guarantee_Matrix%28PGM%29/performance-guarantee-matrix.md) for measured-performance guarantees with a calculation-sheet-per-guarantee structure, an exclusions-asymmetry table, and shadow-calculation requirements. New skill `performance-guarantee-matrix`; `warranty-obligation-matrix` skill re-scoped. The toolkit is now ten satellite documents; README sequence and dependencies updated.
* **Renamed** all satellite document templates to drop the legacy `NN-` numeric filename prefixes (e.g. `04-kpi-definitions.md` → `kpi-definitions.md`) and retired the document numbers everywhere: skill template paths, README links and Resources table (# column dropped), AGENTS.md, and the Metrics Tree's internal links.
* **Added** a "Using with AI agents" section to the [README](/README.md) with Claude Code and Codex setup instructions.

*(Changes ported from the toolkit's first field use.)*
