# Todo

- [ ] Ratify the `❓` canonical models (PCS first, then Meter, Bat, BESS, TX, Weather_Station) in an ontology session. Owner: maintainer.
- [ ] Wire `metric_code` between the Metrics Tree and the ontology's metric registry once the taxonomy session ratifies the ID scheme (the MT template notes `metric_code` stays blank until then). Owner: maintainer.
- [ ] Decide whether the Data Interface Register template should gain an explicit "ONT term" column on its signal rows, or keep referencing terms informally. Owner: maintainer.
