# Definitions (DEF)

Global glossary for the project repo: every acronym and term used anywhere in the documentation, with its project-specific meaning. **Renamed from `Definitions_Taxonomy(DT)` on 2026-08-31**: the taxonomy (naming conventions, segment hierarchy, ID schemes) moved to [`Ontology(ONT)/taxonomy.md`](../Ontology%28ONT%29/taxonomy.md), where segment structure and naming now have one home. This folder is the **prose authority** (what words mean, especially where two contracts disagree); the ontology is the **identifier authority**.

* [Definitions](definitions.md) - Global glossary template: every acronym, term, and organization used across the repo, with project-specific meaning and ambiguous terms flagged.

## Status

* **Template only.** Populate via the `definitions-taxonomy` skill (full-repo scan + two-tier roll-up from the per-document extractions). The taxonomy lives in [`Ontology(ONT)/taxonomy.md`](../Ontology%28ONT%29/taxonomy.md) since 2026-08-31, maintained through the `ontology` session.
